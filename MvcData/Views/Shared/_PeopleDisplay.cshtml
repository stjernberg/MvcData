@model Person

<div class="row">
    <div class="col-5">@Model.Name</div>
    <div class="col-5">@Model.City </div>
    <div class="col-2">@Html.ActionLink("Details", "Details", "Cars", new { id = Model.Id })</div>
</div>
